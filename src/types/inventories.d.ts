type InventoryVariant = {
  id: string
  quantity: number
  createdAt: string
  updatedAt: string
  variant: Variant
}

type Inventory = {
  id: string
  lockedAt: string
  createdAt: string
  updatedAt: string
}
